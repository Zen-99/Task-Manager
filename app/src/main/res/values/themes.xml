<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TaskManager" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="WhiteSearchViewStyle" parent="Widget.AppCompat.SearchView">
        <item name="android:editTextColor">@color/sub_white</item>
    </style>

    <style name="InputLabel">
        <item name="android:editTextColor">@color/sub_black</item>
        <item name="android:fontFamily">@font/dmsans_bold</item>
        <item name="android:textSize">14dp</item>
    </style>
    <style name="InputBox">
        <item name="android:editTextColor">@color/black</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
    </style>




    <style name="Theme.TaskManager" parent="Base.Theme.TaskManager" />
</resources>